import { ElementRef } from '@angular/core';
import { NgxMatHoverScrollDirective } from './ngx-mat-hover-scroll.directive';

describe('NgxMatHoverScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMatHoverScrollDirective();
    expect(directive).toBeTruthy();
  });
});
